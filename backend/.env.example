# Environment Configuration for Cargo Capital Backend

# Storage Configuration
# Options: 'memory', 'file', 'mongodb'
STORAGE_TYPE=mongodb

# MongoDB Configuration
# Replace with your actual MongoDB connection string for the "cargo-capital" database
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/cargo-capital?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:5174

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security
API_KEY=your-api-key-here

# Mail (Contact & Quote forms)
# Use your email and 16-digit App Password (Gmail: Account → Security → 2-Step Verification → App passwords)
MAIL=your-email@gmail.com
EMAIL_APP_PASSWORD=your-16-digit-app-password
# Optional: where to receive contact/quote notifications (defaults to MAIL)
# ADMIN_EMAIL=admin@yourdomain.com

# Alternative names (same as above): EMAIL_USER / EMAIL_PASS
# For custom SMTP (e.g. Zoho): set EMAIL_HOST, EMAIL_PORT, EMAIL_SECURE
