# Environment Configuration for Cargo Capital Backend

# Storage Configuration
# Options: 'memory', 'file', 'mongodb'
STORAGE_TYPE=mongodb

# MongoDB Configuration
# Replace with your actual MongoDB connection string for the "cargo-capital" database
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/cargo-capital?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:5174

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security
API_KEY=your-api-key-here

# Email (SMTP) Configuration
# Choose either generic SMTP (recommended for Zoho) or service name (Gmail, etc.)

# Generic SMTP (Zoho example)
# For Zoho Mail: Use host smtp.zoho.com (or smtp.zoho.in for India),
# port 465 (secure true) or 587 (secure false + STARTTLS)
# EMAIL_HOST=smtp.zoho.com
# EMAIL_PORT=465
# EMAIL_SECURE=true
# EMAIL_USER=your-address@your-domain.com
# EMAIL_PASS=your-app-password-or-application-specific-password
# EMAIL_FROM=Your Brand <your-address@your-domain.com>
# ADMIN_EMAIL=alerts@your-domain.com

# Or using a service name (fallback)
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
# EMAIL_FROM=Your Brand <your-email@gmail.com>
